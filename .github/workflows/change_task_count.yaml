name: Change Task Count

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        description: What environment to change the task count for
        options:
          - dev
          - staging
          - prod
      new_count:
        description: 1 to turn on 0 to turn off
        required: true
      application_name:
        type: choice
        description: What application to adjust the task count for
        options:
          - ingestion
          - rail-performance-manager
          - bus-performance-manager
          - ad-hoc
          - lightswitch

jobs:
  set_count:
    if: |
      ( github.event.inputs.new_count == 0 || github.event.inputs.new_count == 1)

    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: us-east-1
      - name: Run ECS Update Service Command
        # yamllint disable rule:line-length
        run: >
          aws ecs update-service
          --cluster lamp
          --service lamp-${GITHUB_EVENT_INPUTS_APPLICATION_NAME}-${GITHUB_EVENT_INPUTS_ENVIRONMENT}
          --desired-count ${GITHUB_EVENT_INPUTS_NEW_COUNT}
        env:
          GITHUB_EVENT_INPUTS_APPLICATION_NAME: ${{ github.event.inputs.application_name }}
          GITHUB_EVENT_INPUTS_ENVIRONMENT: ${{ github.event.inputs.environment }}
          GITHUB_EVENT_INPUTS_NEW_COUNT: ${{ github.event.inputs.new_count }}
        # yamllint enable
